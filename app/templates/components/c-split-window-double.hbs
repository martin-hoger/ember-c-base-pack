{{#unless hideFirstWindow}}
  <div class="win win-1" style={{win1Style}}>
    {{yield}}
  </div>
{{/unless}}
{{#unless hideSecondWindow}}
  <div class="win win-2" style={{win2Style}}>
    {{yield to="inverse"}}
  </div>
{{/unless}}
